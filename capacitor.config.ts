import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FindProfWorker_List',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
